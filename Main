
public class Main {
    public static void main(String[] args) {
        String password = "Baacaacaacaacaacaa112";
        PasswordChecker checker = new PasswordChecker(password);
        int minChanges = checker.minimumChanges();

        if (minChanges == 0) {
            System.out.println("The password is already strong!");
        } else {
            System.out.println("The password requires " + minChanges + " changes to make it strong.");
        }
    }
}

